diff --git a/node_modules/mcp-handler/dist/index.js b/node_modules/mcp-handler/dist/index.js
index 1234567..abcdefg 100644
--- a/node_modules/mcp-handler/dist/index.js
+++ b/node_modules/mcp-handler/dist/index.js
@@ -470,7 +470,8 @@ function initializeMcpApiHandler(initializeServer, serverOptions = {}, config =
               method: request.method,
               url: request.url,
               headers: request.headers,
-              body: request.body
+              body: request.body,
+              auth: request.auth
             });
             const syntheticRes = new EventEmittingResponse(
               req2,
@@ -592,7 +593,8 @@ function initializeMcpApiHandler(initializeServer, serverOptions = {}, config =
           url: req.url || "",
           method: req.method || "",
           body: parsedBody,
-          headers: Object.fromEntries(req.headers.entries())
+          headers: Object.fromEntries(req.headers.entries()),
+          auth: req.auth
         };
         let timeout = null;
         let hasResponded = false;
diff --git a/node_modules/mcp-handler/dist/index.mjs b/node_modules/mcp-handler/dist/index.mjs
index 1234567..abcdefg 100644
--- a/node_modules/mcp-handler/dist/index.mjs
+++ b/node_modules/mcp-handler/dist/index.mjs
@@ -464,7 +464,8 @@ function initializeMcpApiHandler(initializeServer, serverOptions = {}, config =
               method: request.method,
               url: request.url,
               headers: request.headers,
-              body: request.body
+              body: request.body,
+              auth: request.auth
             });
             const syntheticRes = new EventEmittingResponse(
               req2,
@@ -586,7 +587,8 @@ function initializeMcpApiHandler(initializeServer, serverOptions = {}, config =
           url: req.url || "",
           method: req.method || "",
           body: parsedBody,
-          headers: Object.fromEntries(req.headers.entries())
+          headers: Object.fromEntries(req.headers.entries()),
+          auth: req.auth
         };
         let timeout = null;
         let hasResponded = false;
