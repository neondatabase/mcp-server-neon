diff --git a/node_modules/mcp-handler/dist/index.js b/node_modules/mcp-handler/dist/index.js
index 1529955..750d8c5 100644
--- a/node_modules/mcp-handler/dist/index.js
+++ b/node_modules/mcp-handler/dist/index.js
@@ -697,6 +697,12 @@ function createFakeIncomingMessage(options = {}) {
   req.method = method;
   req.url = url;
   req.headers = headers;
+  // Build rawHeaders array from headers object for @hono/node-server compatibility
+  const rawHeaders = [];
+  for (const [key, value] of Object.entries(headers)) {
+    rawHeaders.push(key, value);
+  }
+  req.rawHeaders = rawHeaders;
   const auth = options.auth || getAuthContext();
   if (auth) {
     req.auth = auth;
@@ -737,16 +743,17 @@ function createServerResponseAdapter(signal, fn) {
         throw new Error("Encoding not supported");
       }
       if (chunk instanceof Buffer) {
-        throw new Error("Buffer not supported");
+        chunk = new Uint8Array(chunk);
       }
+      const encodedChunk = chunk instanceof Uint8Array ? chunk : new TextEncoder().encode(chunk);
       if (!wroteHead) {
         writeHead(statusCode, headers);
       }
       if (!controller) {
-        bufferedData.push(new TextEncoder().encode(chunk));
+        bufferedData.push(encodedChunk);
         return true;
       }
-      controller.enqueue(new TextEncoder().encode(chunk));
+      controller.enqueue(encodedChunk);
       return true;
     };
     const eventEmitter = new events.EventEmitter();
diff --git a/node_modules/mcp-handler/dist/index.mjs b/node_modules/mcp-handler/dist/index.mjs
index 4eeee17..48c50ed 100644
--- a/node_modules/mcp-handler/dist/index.mjs
+++ b/node_modules/mcp-handler/dist/index.mjs
@@ -691,6 +691,12 @@ function createFakeIncomingMessage(options = {}) {
   req.method = method;
   req.url = url;
   req.headers = headers;
+  // Build rawHeaders array from headers object for @hono/node-server compatibility
+  const rawHeaders = [];
+  for (const [key, value] of Object.entries(headers)) {
+    rawHeaders.push(key, value);
+  }
+  req.rawHeaders = rawHeaders;
   const auth = options.auth || getAuthContext();
   if (auth) {
     req.auth = auth;
@@ -731,16 +737,17 @@ function createServerResponseAdapter(signal, fn) {
         throw new Error("Encoding not supported");
       }
       if (chunk instanceof Buffer) {
-        throw new Error("Buffer not supported");
+        chunk = new Uint8Array(chunk);
       }
+      const encodedChunk = chunk instanceof Uint8Array ? chunk : new TextEncoder().encode(chunk);
       if (!wroteHead) {
         writeHead(statusCode, headers);
       }
       if (!controller) {
-        bufferedData.push(new TextEncoder().encode(chunk));
+        bufferedData.push(encodedChunk);
         return true;
       }
-      controller.enqueue(new TextEncoder().encode(chunk));
+      controller.enqueue(encodedChunk);
       return true;
     };
     const eventEmitter = new EventEmitter();
