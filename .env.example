# Live Neon E2E tests only.
# Use an organization-scoped API key for a dedicated disposable test org.
NEON_API_KEY=
# Optional for organization-scoped keys (inferred from the key when omitted).
# Required when using a user API key.
NEON_TEST_ORG_ID=
